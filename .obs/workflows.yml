---
staging_build:
  steps:
  filters:
    event: pull_request

refresh_staging_project:
  steps:
    - trigger_services:
        project: home:defolos:BCI:CR:SLE-15-SP4
        package: _project
  filters:
    event: push
    branches:
      only:
        - sle15-sp4


refresh_devel_BCI:
  steps:
  filters:
    event: push
    branches:
      only:
        - sle15-sp4
